#!/usr/bin/env bash
# Author: Aaron Kuehler
# Purpose: Configures the shell

# Add pretty colors
export CLICOLOR=1

export EDITOR=emacs

for file in $DOTFILES_THIRD_PARTY_HOME/*; do
  source $file;
done
