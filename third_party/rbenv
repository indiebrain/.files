#/usr/bin/env bash
# Author: Aaron Kuehler
# Purpose: Configure shell to use rbenv

# Use Homebrew installed rbenv
RBENV_ROOT=/usr/local/var/rbenv

# Initialize rbenv
if which rbenv > /dev/null; then eval "$(rbenv init -)"; fi
