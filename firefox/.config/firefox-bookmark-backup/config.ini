  [backup]
  # Directory where backups will be written
  destination = ~/Sync/Documents/Bookmarks

  # Maximum number of backup files to keep (oldest are deleted first)
  max_backups = 10

  # Firefox profile path (leave blank to auto-detect Developer Edition)
  profile =
